import React from 'react/addons';
import ReactOauth2Login from '../lib/react-oauth2-login.jsx';

describe('ReactOauth2Login', function() {
  var component;

  beforeEach(function() {
    component = React.addons.TestUtils.renderIntoDocument(
      <ReactOauth2Login/>
    );
  });

  it('should render', function() {
    expect(React.findDOMNode(component).className).toEqual('react-oauth2-login');
  });
});
